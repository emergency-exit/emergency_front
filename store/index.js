export const state = () => ({});

export const mutations = {};

// export const actions = {
//   // 서버 사이드 렌더링에서 핵심
//   // layout에 fetch를 사용할 수가 없음
//   // 서버사이드 렌더링에서는 localStorage 사용 못함
//   nuxtServerInit({ commit, dispatch, state }, { req }) {
//     return dispatch("members/getMyInfo");
//   }
// };
